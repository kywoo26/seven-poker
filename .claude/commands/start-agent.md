---
description: 에이전트 시작 명령어 생성
argument-hint: <agent-name>
---

새 터미널에서 에이전트를 시작하기 위한 명령어를 생성합니다.

## Arguments
- `agent-name`: 시작할 에이전트 (game-logic, frontend, backend, devops)

## What to do

### 1. Check Prerequisites
- worktree가 존재하는지 확인
- 에이전트가 IDLE 상태인지 확인
- 할당된 Issue가 있는지 확인

### 2. Generate Start Command
새 터미널에서 실행할 명령어를 출력합니다:

```bash
cd C:\Users\K\dev\github\seven-poker\.worktrees\<agent-name>
claude "
당신은 Seven Poker 프로젝트의 <agent-name> 에이전트입니다.

## 작업
Issue #<issue-number>: <issue-title>

## 규칙
1. .claude/agents/<agent-name>.md 파일의 규칙을 따르세요
2. CLAUDE.md의 프로젝트 가이드라인을 준수하세요
3. 개발 완료 후 PR을 생성하세요
4. PR 생성 후 state.json을 PR_PENDING으로 업데이트하세요

## 시작하기
먼저 Issue 내용을 확인하세요: gh issue view <issue-number>
"
```

### 3. Update State
state.json에서 에이전트 상태를 DEVELOPING으로 업데이트합니다.

### 4. Output

```
🚀 Agent Start Command Generated
══════════════════════════════════════════════════════════════════════════

Agent: <agent-name>
Issue: #<issue-number> - <issue-title>
Worktree: .worktrees/<agent-name>

📋 새 터미널에서 다음 명령어를 실행하세요:

┌──────────────────────────────────────────────────────────────────────────┐
│ cd C:\Users\K\dev\github\seven-poker\.worktrees\<agent-name>             │
│ claude "<prompt...>"                                                     │
└──────────────────────────────────────────────────────────────────────────┘

또는 PowerShell에서:
  Start-Process wt -ArgumentList "new-tab", "-d", ".worktrees/<agent-name>"

══════════════════════════════════════════════════════════════════════════
```
